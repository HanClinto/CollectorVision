.DS_Store
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv*/
# Model weights and gallery databases are NOT stored in regular git.
# Weights: tracked via git LFS (collector_vision/weights/*.pt).
# Galleries: distributed via GitHub Releases and HF Datasets.
# The built wheel DOES include the weights via package_data.
*.npz
*.db
.env
